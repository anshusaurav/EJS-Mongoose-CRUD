<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        
        <title>Index</title>
    </head>
    <body>
        <h2>Users</h2>
         <% users.forEach(elem => { %>
                 <a href="users/<%= elem.email%>"><p><%= elem.name%></p></a>
                 <p><%= elem.email%></p>
                 <p><%= elem.createdAt%></p>
            <% })%>
    </body>
</html>



